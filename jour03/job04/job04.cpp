#include <iostream>
#include <algorithm>

int main(){
    std::string str = "vive la plateforme !";
    int size = str.size();

    std::cout << "Taille de la chaine de caractere : " << size << std::endl;
    
    return 0;
}